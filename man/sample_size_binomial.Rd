% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{sample_size_binomial}
\alias{sample_size_binomial}
\title{Sample Size Calculation for a Binomial Proportion}
\usage{
sample_size_binomial(
  x = NULL,
  p = NULL,
  epsilon,
  conf = 0.95,
  N = NULL,
  relative = FALSE
)
}
\arguments{
\item{x}{Numeric vector of 0/1 data, or NULL if user provides p.}

\item{p}{Numeric. Optional expected proportion of "successes" (0 < p < 1). If NULL, estimated from x.}

\item{epsilon}{Numeric. Desired precision (margin of error, 0 < E < 1).}

\item{conf}{Numeric. Confidence level (default = 0.95).}

\item{N}{Optional. Population size for finite population correction (default = NULL).}

\item{relative}{Logical. If TRUE, interprets E as relative error (default = FALSE).}
}
\value{
Required sample size (integer).
}
\description{
Computes the required sample size to estimate a proportion in a binomial
distribution with a given absolute or relative error and confidence level.
}
\examples{
# Example with observed data and absolute precision
y <- c(rep(1, 30), rep(0, 70))
sample_size_binomial(x = y, epsilon = 0.05)

# Example with relative precision (5\% of the proportion)
sample_size_binomial(p = 0.4, epsilon = 0.05, conf = 0.95, N = 500, relative = TRUE)
}
